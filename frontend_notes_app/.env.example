# Frontend environment configuration for the Notes app

# Base URL for the backend API (no trailing slash). Example:
# http://localhost:3001
# or a full https URL when deployed.
REACT_APP_API_BASE=http://localhost:3001

# Health check path exposed by backend (should start with a slash)
REACT_APP_HEALTHCHECK_PATH=/api/health
